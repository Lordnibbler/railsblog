//= link_tree ../images

// NOTE: we used to use sprockets for JS and CSS. As of rails 6, webpack is used for JS by default.
//       we have also opted to use webpack for CSS for simplicity sake, so we removed the link_directory
//       directives for javascripts and stylesheets in /app/assets
