<div x-data="{
    triggerNavItem(id) {
        window.smoothScrollTo(id, 40)
    },
    triggerHomeItem() {
        window.smoothScrollTo(0)
    },
    triggerMobileNavItem(id) {
        $parent.mobileMenu = false;
        this.triggerNavItem(id)
    },
    triggerMobileHomeItem() {
        $parent.mobileMenu = false;
        this.triggerHomeItem()
    }
}">
    <% # desktop navigation %>
    <div class="desktop-nav fixed transition ease-in-out duration-300 w-full z-50 top-0 py-3 sm:py-5 <%= navigation_class %>">
        <div class="navigation-container flex justify-between items-center">
            <div>
                <a href="/" <%= tag.attributes(home_navigation_attributes('triggerHomeItem()')) %>
                    class="uppercase text-white text-2xl sm:text-4xl lg:text-2xl">
                    <span class="font-black">Radler</span> <span class="font-thin">Designs</span>
                </a>
            </div>

            <div class="hidden lg:block">
                <ul class="flex items-center">
                    <%= home_desktop_navigation_link(name: "home") %>
                    <%= desktop_navigation_link(name: "lab",         path: lab_path) %>
                    <%= desktop_navigation_link(name: "photography", path: photography_path) %>
                    <%= desktop_navigation_link(name: "blog",        path: blog_posts_path) %>
                    <%= scrolling_desktop_navigation_link(name: "about",      path: "#about") %>
                    <%= scrolling_desktop_navigation_link(name: "work",       path: "#work") %>
                    <%= scrolling_desktop_navigation_link(name: "games",      path: "#games") %>
                    <%= scrolling_desktop_navigation_link(name: "videos",     path: "#videos") %>
                    <%= scrolling_desktop_navigation_link(name: "latest",     path: "#latest") %>
                    <%= scrolling_desktop_navigation_link(name: "contact",    path: "#contact") %>
                </ul>
            </div>

            <% # hamburger icon %>
            <div class="block lg:hidden">
                <button class="mobile-nav-toggle text-white" x-ref="mobileToggle"
                    @click="$parent.mobileMenu = !$parent.mobileMenu; if ($parent.mobileMenu) $nextTick(() => $refs.mobilePanel.querySelector('a').focus())"
                    aria-expanded="false"
                    :aria-expanded="$parent.mobileMenu.toString()"
                    aria-label="Open navigation"
                    x-bind:aria-label="$parent.mobileMenu ? 'Close navigation' : 'Open navigation'">
                    <span class="mobile-nav-toggle-icon" aria-hidden="true"
                        :class="{ 'mobile-nav-toggle-icon-open': $parent.mobileMenu }"></span>
                </button>
            </div>
        </div>
    </div>

    <% # mobile navigation %>
    <div class="mobile-nav fixed inset-0 bg-black bg-opacity-40 backdrop-blur-sm z-40 min-h-full lg:hidden opacity-0 pointer-events-none overflow-hidden"
        :class="{ 'opacity-100 pointer-events-auto mobile-nav-open': $parent.mobileMenu }"
        inert :inert="!$parent.mobileMenu"
        aria-hidden="true" :aria-hidden="(!$parent.mobileMenu).toString()"
        @click.self="$parent.mobileMenu = false; $nextTick(() => $refs.mobileToggle.focus())"
        @keydown.escape.window="if ($parent.mobileMenu) { $parent.mobileMenu = false; $nextTick(() => $refs.mobileToggle.focus()) }"
        @wheel.self.prevent @touchmove.self.prevent>
        <div class="mobile-nav-panel mobile-glass-panel py-5 px-6 sm:px-8" x-ref="mobilePanel"
            role="dialog" aria-modal="true" aria-label="Site navigation">
            <ul class="mobile-nav-list flex flex-col">
                <%= home_mobile_navigation_link(name: 'home') %>
                <%= mobile_navigation_link(name: 'lab',         path: lab_path) %>
                <%= mobile_navigation_link(name: 'photography', path: photography_path) %>
                <%= mobile_navigation_link(name: "blog",        path: blog_posts_path) %>
                <%= scrolling_mobile_navigation_link(name: "about",      path: "#about") %>
                <%= scrolling_mobile_navigation_link(name: "work",       path: "#work") %>
                <%= scrolling_mobile_navigation_link(name: "games",      path: "#games") %>
                <%= scrolling_mobile_navigation_link(name: "videos",     path: "#videos") %>
                <%= scrolling_mobile_navigation_link(name: "latest",     path: "#latest") %>
                <%= scrolling_mobile_navigation_link(name: "contact",    path: "#contact") %>
            </ul>
        </div>
    </div>
</div>
